{
  "name": "react-journey-monorepo",
  "private": true,
  "scripts": {
    "build-frontend": "cd frontend && npm install && npm run build",
    "start-backend": "cd backend && npm install && node index.js"
  }
}
